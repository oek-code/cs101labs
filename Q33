int isbn = 9000 + (int)(Math.random() * 1000);

// 1000 kez deneme hakkı
for (int i = 0; i < 1000; i++) {
    if (!books.contains("ISBN" + isbn)) {
        // benzersiz ISBN bulundu
        break;  // bu break sadece for'dan çıkmak için
    }
    isbn = 9000 + (int)(Math.random() * 1000);
}
